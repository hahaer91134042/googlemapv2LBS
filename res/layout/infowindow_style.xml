<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/RelativeLayout1"
    android:layout_width="150dp"
    android:layout_height="wrap_content"
    android:background="@drawable/infostyle"
    android:orientation="horizontal" >

    <ImageView
        android:id="@+id/infoImg"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true" />

    <TextView
        android:id="@+id/infoTitle"
        android:layout_width="75dp"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/infoImg"
        android:layout_toRightOf="@+id/infoImg"
        
        android:textColor="@drawable/aqua"
        android:textSize="15sp" />

    <TextView
        android:id="@+id/infoContent"
        android:layout_width="100dp"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_below="@+id/infoImg"
        android:textColor="@drawable/fuchsia"
        android:textSize="10sp" />

</RelativeLayout>
